{
  "name": "sky-island-obby",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sky Island Obby — a Roblox-style 3D obstacle-course game for iPad and desktop browsers",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "icons": "node scripts/make-icons.mjs"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/three": "^0.171.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
